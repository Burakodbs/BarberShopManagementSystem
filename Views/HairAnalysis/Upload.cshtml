<div class="container mt-5 d-flex justify-content-center">
    <div class="card p-4" style="width: 100%; max-width: 500px; border-radius: 15px; box-shadow: 0px 8px 20px rgba(0, 0, 0, 0.15); background: linear-gradient(135deg, #f5f7fa, #c3cfe2);">
        <div class="card-body text-center">
            <h3 class="mb-3" style="font-family: 'Poppins', sans-serif; font-weight: 700; color: #333;">Saç Analizi</h3>
            <p class="mb-4" style="font-family: 'Poppins', sans-serif; font-size: 16px; color: #555;">Kendi fotoğrafınızı yükleyerek saç analizinizi yaptırabilirsiniz. Hemen deneyin!</p>

            <form asp-action="AnalyzeHair" method="post" enctype="multipart/form-data">
                <div class="mb-4">
                    <label for="imageFile" class="form-label" style="font-family: 'Poppins', sans-serif; font-weight: 500; color: #444;">Fotoğraf Yükle</label>
                    <input type="file" name="imageFile" id="imageFile" class="form-control" accept="image/*" required
                           style="border: 2px dashed #007bff; border-radius: 10px; padding: 15px; background-color: #f9f9f9;"
                           onchange="previewImage(event)">
                    <img id="imagePreview" class="mt-3" style="max-width: 100%; max-height: 200px; display: none; border-radius: 10px; box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1);" />
                </div>

                <button type="submit" class="btn btn-primary w-100" style="background-color: #007bff; border: none; padding: 15px; font-size: 16px; font-weight: 600; border-radius: 10px; text-transform: uppercase; transition: all 0.3s;">
                    Analiz Et
                </button>
            </form>
        </div>
    </div>
</div>

<script>
    function previewImage(event) {
        const preview = document.getElementById('imagePreview');
        preview.src = URL.createObjectURL(event.target.files[0]);
        preview.style.display = 'block';
    }
</script>
